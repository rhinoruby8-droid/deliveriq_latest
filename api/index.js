// This placeholder file is tracked in Git to register the Serverless Function.
// It will be overwritten by Vite during the 'npm run build' phase.
export default function handler(req, res) {
  res.status(200).json({ status: "building" });
}
